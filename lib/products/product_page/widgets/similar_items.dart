import 'package:flutter/material.dart';

class SimilarItems extends StatelessWidget {
  const SimilarItems({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: SizedBox(
        height: 400,
        child: Center(
          child: Text('Вам может понравится'),
        ),
      ),
    );
  }
}
